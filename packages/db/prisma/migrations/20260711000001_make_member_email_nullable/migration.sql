-- -----------------------------------------------------------------------------
-- 参加者メールを任意化
-- 参加者はスケジュール上の担当者として登録するもので、メール登録は必須ではない。
-- (メールは将来フェーズで「予定変更時の共有リンク自動送信」に使用予定)
--
-- NULL 許可により:
--   - メール未登録の参加者を複数登録できる (uq_pm_project_email は Postgres の
--     NULL DISTINCT 既定挙動により NULL 同士を重複扱いしないため衝突しない)
--   - ck_pm_email_length CHECK は NULL のとき評価結果 NULL → 制約は満たされる
--     (空文字 '' は char_length=0 で違反するため、未登録は必ず NULL で保存する)
-- -----------------------------------------------------------------------------

ALTER TABLE "project_members" ALTER COLUMN "email" DROP NOT NULL;
